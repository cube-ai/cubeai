serviceboot:
  version: v2

service:
  cname: CubeAI能力开放网关
  ename: ability
  version: 2.3.0
  port:
    dev: 8104
    prod: 80

gateway:
  is_gateway: yes

build:
  image_name: abilitypy
  build_web: no
  compile_python_to_so: no
